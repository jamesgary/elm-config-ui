{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "elm": "elm make src/Main.elm --output=public/js/compiled/main.js",
    "gen_config": "elm make config/Data.elm --output tmp/gen-config.js > /dev/null && node tmp/gen-config.js > src/Config.elm 2>/dev/null",
    "watch": "npm-watch gen_config"
  },
  "watch" : {
    "elm": "src/*.elm",
    "gen_config": "config/Data.elm"
  }
}
